# find qt dependencies package
find_package(@QtWebApp_QT_FINDPKG_CMDSTR@)

@PACKAGE_INIT@

set(QtWebApp_VERSION_MAJOR "@QtWebApp_VERSION_MAJOR@")
set(QtWebApp_VERSION_MINOR "@QtWebApp_VERSION_MINOR@")
set(QtWebApp_VERSION_PATCH "@QtWebApp_VERSION_PATCH@")

set_and_check(QtWebApp_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIRS@")

include("${CMAKE_CURRENT_LIST_DIR}/QtWebApp-targets.cmake")

# Make sure we find packages for our dependencies
foreach(_PKG IN ITEMS @QtWebApp_PKGS@)
    find_package(${_PKG} REQUIRED)
endforeach ()

set(QtWebApp_LIBRARY QtWebApp::QtWebApp)

check_required_components(@PROJECT_NAME@)
